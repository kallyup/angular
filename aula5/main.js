

const {gets, print} = require('./funcoes');

const numSorteados =[]

for (let index = 1; index < 5; index++) {
    const numSorteados = gets();
    numSorteados.push(numSorteados)
}

print(numSorteados)